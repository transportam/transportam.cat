---
const { class: className = '', ...rest } = Astro.props

const baseClass = 'mx-auto max-w-7xl px-4 sm:px-6 lg:px-8'
const finalClass = [baseClass, className].filter(Boolean).join(' ')
---

<div class={finalClass} {...rest}>
  <slot />
</div>
